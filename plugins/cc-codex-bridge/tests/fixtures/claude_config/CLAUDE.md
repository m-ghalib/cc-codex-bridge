# Test Project

Instructions for the test project.

## Tools

Use the `Edit` tool for file modifications. Use `WebFetch` to retrieve URLs.
Use `Bash` to run shell commands. Use `Read` to view file contents.

## Rules

- Always run `uv run pytest` before committing
- Keep functions under 50 lines
