# Platform config dirs at repo root (never commit — these are the OUTPUT, not source)
/.claude/
/.codex/
/.agents/
/.gemini/

# Node
node_modules/
build/
*.tsbuildinfo

# Build outputs — ignore top-level dist but commit plugin runtime artifacts.
/dist/
!/plugins/cc-codex-bridge/dist/

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Environment
.env
.env.local
